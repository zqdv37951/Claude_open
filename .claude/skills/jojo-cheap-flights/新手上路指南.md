# 揪揪漫遊｜JojoNowhere 機票查詢台 新手上路指南

給第一次接觸這套「機票查詢台＋Claude比價」工作流的人看的。技術細節不重複寫在這裡，都指向 [`SKILL.md`](SKILL.md) 跟 [`references/data-format.md`](references/data-format.md)（本文每一段都有連結）。

---

## 先看這個：這不是找「最便宜」，是找「真的划算」

**這個工具幫你判斷一件事**：現在這個價格，跟這條航線自己過去的真實報價比起來，算不算划算。

原因很簡單：一張 NT$8,000 的票，如果這條航線過去 61 天平均都在 NT$6,000 上下，其實是貴的；一張 NT$15,000 的票，如果這條航線平常都要 NT$20,000 以上，反而是難得的好價格。**光看數字大小看不出這件事，要跟歷史比才知道。**

工具會拿每一筆報價去跟 Google Flights 自己算出來的過去約 61 天真實每日票價比對，算出一個 **PR 值**（跟你考試成績單上的 PR 是同一個概念——這張票贏過過去幾 % 的日子，PR 越高代表現在買越划算）。這個 PR 值，比單純看「哪個數字最小」更能回答你真正想知道的問題。

---

## Claude 會先問清楚，這是正常的

貼查詢規格（不管是查詢台網頁生成的 JSON，還是你自己打字描述的需求）給 Claude 之後，**它通常不會馬上動手查，會先確認幾件事**：

- 起站/終點站如果寫的是「開放範圍」（例如「中國」「東南亞」），Claude 會先決定要查哪幾個具體機場，並且講清楚它的假設——不會真的把一整個國家每個機場都查一次（那會浪費大量查詢額度），通常會挑幾個有實際航線連結的主要城市。
- 如果一次要查的組合數很多（例如很多個日期 × 很多個目的地），Claude 會先講清楚大概要查幾次、需不需要用到 Bright Data 的額度。
- 查到「資料量太少、算不出優惠%」的航線時，Claude 會老實講清楚是哪種情況（後面「已知限制」那一節有列），不會硬編一個數字給你。

**這些確認都是為了避免查錯範圍、白白浪費查詢次數**——多問一句話的成本，遠比查了一大批結果才發現方向不對低。

---

## 安裝需求

大多數人**不需要先裝任何東西**——查詢會先直接連 Google Flights（用一般瀏覽器的方式，沒有 API key、不用註冊），偶爾查一兩次通常就會成功。以下工具只有在「查詢量比較大、直接連被 Google 暫時擋下來」時才用得到：

| 用途 | 工具 | 備註 |
|---|---|---|
| 執行查詢腳本 | Python 3 | 大多數電腦已經內建，或用 `brew install python3` |
| 產生 Google Flights 查詢網址 | `fast-flights`、`typing_extensions`、`primp` | `pip install fast-flights typing_extensions primp` |
| 一般 HTTP 請求 | `requests` | `pip install requests` |
| 大量查詢的備援管道（選用） | Bright Data 帳號 | 免費申請、開一個 **SERP API** 類型的 zone（不是 Web Unlocker／Browser API），每月 5,000 次免費額度；跟這個工具的作者無關，是 Bright Data 自己的免費方案 |

沒有裝 Bright Data 不影響一般使用，只有真的被擋住時，Claude 會告訴你要不要設定這個當備援。

---

## 怎麼開啟「查詢台」

`assets/flight_query_console.html` 是一個自包含的靜態網頁，不需要伺服器。兩種開法都行：

1. **直接在瀏覽器打開**：雙擊這個檔案，或用瀏覽器開啟它的本機路徑，功能完全一樣。
2. **請 Claude（claude.ai）用 Artifact 側邊欄打開**：把檔案內容貼給 Claude 請它渲染成 Artifact，會有內嵌側邊欄的介面，方便邊聊邊調——這是這套工具原本開發、測試時用的環境。

大部分人其實不需要打開這個網頁——直接跟 Claude 用文字描述你想查的航線、日期、預算，一樣可以查，網頁只是給你一個更視覺化的方式填條件。

---

## 查詢台介面導覽

四張卡片，對應四個可以查詢的屬性，每一個都能自己選要「固定」「給清單」還是「開放」：

- **時間**：固定某一天、給一段日期範圍（依取樣頻率抽查），或完全開放讓引擎在未來幾個月內自己探索。
- **價格**：查完之後用來排序、判斷優惠程度的依據——設定最低優惠門檻(PR 值/相對歷史均價的百分比)、排序方式、直飛或轉機。
- **起站範圍**：輸入一個或多個機場代碼，或以某個機場為中心、給一個範圍（同城市／同國家／鄰近區域）讓引擎自己找附近機場。
- **終點站範圍**：跟起站邏輯一樣，多一個「開放探索」模式——用一句話描述想去的地方（例如「有雪的地方」「東南亞」），交給 Claude 判讀，不是選單。

**總查詢量 ≈ 時間的組合數 × 起站的組合數 × 終點站的組合數。三個裡面最多開放一個，不然組合數會爆炸**，畫面上會用顏色提醒你目前的設定會查幾次、划不划算。

按「查詢」會生成一段規格（JSON），**這個網頁本身沒辦法安全地帶著任何金鑰直接連外部服務**——下一步永遠是把這段規格貼給 Claude，請它真的去查。

---

## 建議工作流程

1. 打開查詢台（或直接用文字跟 Claude 描述你的需求），設定時間/起站/終點站/價格門檻。
2. 按「查詢」，複製生成的規格，貼給 Claude。
3. **Claude 會先確認範圍跟查詢量**（尤其是「開放」模式的起站/終點站）——這是正常流程，確認完才會開始查。
4. Claude 實際跑查詢，回報每一筆結果：日期、起降時間、飛行時長、價格、PR 值、航空公司、訂票連結。
5. 查到「資料不足」或「查無班機資料」的航線，Claude 會照實講清楚原因，不會幫你編一個數字。
6. 想要看歷史紀錄整理成頁面，可以請 Claude 把結果寫回查詢台網頁，之後每次查詢都會累積在下面，摺疊收好。

---

## PR 值小辭典

查詢結果裡幾個常見的詞，先搞懂這幾個就夠用：

- **PR 值**——這張票贏過過去 61 天裡幾 % 的日子。PR 90 代表過去 61 天中，有 90% 的日子比現在貴，數字越高越划算。
- **典型價格區間**——Google Flights 自己標示的「正常價格」範圍，落在這個區間內算「typical」，低於算「low」，高於算「high」。
- **歷史均價／中位數**——過去 61 天報價的平均值跟中間值，兩個都給是因為極端值（例如一天突然的促銷或暴漲）會拉歪平均值，中位數比較不受影響。
- **95% 信賴區間**——假設價格分佈接近常態分佈，算出來的「正常波動範圍」，超出這個範圍的價格算是比較罕見的極端值。
- **比歷史均價便宜多少%**——最直觀的一個數字，正的代表比平常便宜，負的代表比平常貴。

---

## 已知限制與常見問題（不是 bug，是資料來源的環境特性）

- **俄羅斯航空（Aeroflot）等被西方平台下架的航空公司,不管怎麼查都不會出現。** Google Flights 自己的資料源就沒有收錄這些航班——這件事已經實際驗證過（同一條航線、不同地區設定都查不到，但 Trip.com 這類非西方平台查得到）。想找這類航空公司的選項，需要換一個資料源。
- **冷門航線或聖彼得堡這類低流量機場，常常查得到真實價格，但 Google 沒有足夠歷史資料算優惠%。** 這種情況畫面會顯示「資料不足」，不會硬套一個數字。
- **來回票的查詢，資料完整度目前不如單程查詢穩定**——同樣的航線用單程查通常拿得到完整資料，改成來回查有時會整條航線查不到任何報價，遇到這種情況值得直接上 Trip.com 或 Google Flights 網頁版交叉核對一次，不要假設「查不到」就代表「真的沒有」。
- **短時間內查太多次，Google 可能暫時擋住直接查詢。** 這時 Claude 會告訴你需不需要設定 Bright Data 當備援，不用自己猜測是哪裡壞了。
- **典型價的計算方式沒有公開文件**，這個工具用的 PR 值、信賴區間都是自己拿真實資料反推出來的統計量，不是 Google 官方公佈的公式；已知確定的是它抓的是約 60 天的滾動窗口，不是跨年同期比較。

---

延伸閱讀：完整技術規則、資料格式的每一個欄位怎麼來的，見 [`SKILL.md`](SKILL.md) 跟 [`references/data-format.md`](references/data-format.md)。
