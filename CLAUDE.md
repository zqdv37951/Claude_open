# 專案筆記

## GitHub Pages 已啟用

這個 repo（`zqdv37951/Claude_open`）已經開啟 GitHub Pages，從 `main` 分支根目錄部署，網址前綴固定是：

```
https://zqdv37951.github.io/Claude_open/<repo 內路徑，非 ASCII 字元需 URL-encode>
```

push 到 `main` 後會自動重新部署。

**當使用者要某個檔案的「網址」時，預設給這個 GitHub Pages 網址**（把檔案在 repo 裡的路徑接在後面、正確 URL-encode），不要主動給 Artifact 連結或其他代理服務（如 htmlpreview.github.io）。

只有使用者明確說「用 artifacts」或「用 claude 公開網址」時，才改用 Artifact 工具發布連結。

（附注：這個沙盒環境的出站網路政策會擋掉 `*.github.io`，無法在這裡用 curl 驗證連結是否正常，這是環境限制、不代表連結有問題；照樣把網址給使用者，讓他們自己開瀏覽器確認即可。）

## 修改範圍紀律（血淚教訓，務必遵守）

**曾經發生過**：使用者只回報一份檔案的兩個具體問題，我卻自己判斷「這個 bug 存在於多份檔案裡」，未經確認就寫了批次腳本把所有同類檔案、甚至 skill 規格本體都改了一輪。使用者後來要求全部復原。

**規則**：

1. **使用者指名一個具體問題或一份檔案時，動作只限於那個範圍**——即使在別的地方看到一模一樣的問題，也一樣。
2. **發現同類問題出現在別處時，寫在回覆裡告訴使用者「另外 N 個地方也有一樣的問題，要不要一起處理」，然後停下來等回覆**——不要自己接著動手。「我發現了什麼」跟「我接下來該做什麼」是兩件事，中間一定要讓使用者插話。
3. **只有使用者的措辭本身就是廣範圍的**（如「全部」「都」「幫我全面檢查」「全面重新整理」這類字眼），才可以主動把動作套用到多個檔案／整個 repo。範圍不確定時，先問一句比先動手便宜。
4. **每次動手前，想清楚「使用者實際說的範圍」跟「我判斷合理的範圍」是不是同一件事**——這兩者不一致時，以使用者說的為準，把判斷出來的部分講出來而不是做出來。
